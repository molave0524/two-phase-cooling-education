npx lint-staged

# Run build to catch errors before commit
echo "🔨 Running build check..."
npm run build

if [ $? -ne 0 ]; then
  echo "❌ Build failed. Please fix errors before committing."
  exit 1
fi

echo "✅ Build successful"
